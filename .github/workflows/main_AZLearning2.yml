name: Deploy ASP.NET Core App to Azure App Service - AZLearning2

on:
  push:
    branches:
      - main
      - master
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.x'

      - name: Restore
        run: dotnet restore MyAzureWebApp.csproj
        working-directory: src

      - name: Build
        run: dotnet build MyAzureWebApp.csproj -c Release --no-restore
        working-directory: src

      - name: Publish
        run: dotnet publish MyAzureWebApp.csproj -c Release -o publish --no-build
        working-directory: src

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: app
          path: src/publish

  deploy:
    runs-on: ubuntu-latest
    needs: build

    steps:
      - name: Download artifact
        uses: actions/download-artifact@v4
        with:
          name: app

      - name: Deploy to Azure Web App
        uses: azure/webapps-deploy@v3
        with:
          app-name: AZLearning2
          publish-profile: ${{ secrets.AZURE_WEBAPP_PUBLISH_PROFILE }}
          package: .
